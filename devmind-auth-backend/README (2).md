# DevMind Auth Backend

Upload these files at the GitHub repository root. The repo root must directly contain `api`, `lib`, `package.json`, and `vercel.json`.

Test after deploy:

- `/api`
- `/api/health`
- `/api/auth/google/start`
- `/api/auth/github/start`
